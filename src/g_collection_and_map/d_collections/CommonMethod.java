package g_collection_and_map.d_collections;

class CommonMethod {
    public static void main(String[] args) {
        //Collections 是一个可以操作 Collection 和 Map 的工具类

        //1.reverse(List)：逆置 List
        //2.shuffle(List)：对 List 中的元素进行随机排列
        //3.sort(List)：对 List 中的元素进行升序排序
        //4.sort(List, Comparator)：对 List 中的元素以传入的 Comparator 为标准进行升序排序
        //5.swap(List, int, int)：交换 List 中两个位置上的元素
        //6.max(Collection)：返回 Collection 中的最大元素
        //7.max(Collection, Comparator)：以传入的 Comparator 为标准返回 Collection 中的最大元素
        //8.min(Collection)：返回 Collection 中的最小元素
        //9.min(Collection, Comparator)：以传入的 Comparator 为标准返回 Collection 中的最小元素
        //10.frequency(Collection, Object)：统计 Collection 中某个元素的出现次数
        //11.copy(List dest, List src)：复制 src 到 dest（dest 的长度必须长于 src，否则会报错）
        //12.replaceAll(List, Object, Object)：将 List 中所有的旧 Object 替换为新 Object
    }
}
