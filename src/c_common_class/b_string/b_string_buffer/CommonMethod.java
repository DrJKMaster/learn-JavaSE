package c_common_class.b_string.b_string_buffer;

class CommonMethod {
    public static void main(String[] args) {
        //1.append()：追加
        //  如果参数 str = null 则执行父类中的 appendNull 方法，追加 "null"
        //2.delete(int start, int end)：删除 index 为 [start, end) 的字符
        //3.replace(int start, int end, String str)：将 index 为 [start, end) 的字符替换为str
        //4.indexOf(String str)：返回第一个 str 的索引，如未找到，则返回-1
        //5.insert(int offset, String str)：在 offset 处插入 str
        //6.length()：返回字符串长度
    }
}
