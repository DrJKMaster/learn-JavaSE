package c_common_class.c_math;

class CommonMethod {
    public static void main(String[] args) {
        //Math 是工具类，其所有的方法均为静态方法

        //1.abs：求绝对值，返回传入的类型
        //2.pow：求幂，返回 double
        //3.sqrt：开方，返回 double
        //4.ceil：向上取整（返回值 >= 参数），返回 double
        //5.floor：向下取整（返回值 <= 参数），返回 double
        //6.round：四舍五入，传入 float 则返回 int，传入 double 则返回 long
        //7.random：求随机数，返回一个 [0,1) 的 double
        //8.max：求两个数中的最大值，返回传入的类型
        //9.min：求两个数中的最小值，返回传入的类型
        
        //NaN = Not a number：不是数字
    }
}
